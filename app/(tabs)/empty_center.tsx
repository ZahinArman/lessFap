import { View } from 'react-native';

// This is a placeholder screen for the center tab button.
// The actual action is handled by the custom tab bar which opens the log-entry modal.
export default function EmptyCenter() {
  return <View />;
}
